// import {expect} from 'chai';
// import {Map, List, fromJS} from 'immutable';
// import GOOGLE_BOOK from '../data/book';
// import GOOGLE_RESULTS from '../data/books_three';
// import {extractBook, getBooks} from '../results';
//
// describe.only('A book', () => {
//   describe('getBooks', () => {
//     it('should extract a list of books', () => {
//       let books = getBooks(GOOGLE_RESULTS);
//       expect(books.size).to.equal(3);
//       expect(books.get(0).get('title')).to.equal('Mastering Regular Expressions');
//       expect(books.get(1).get('title')).to.equal('Java Regular Expressions');
//       expect(books.get(2).get('title')).to.equal('Regular Expressions Cookbook');
//     });
//   });
//
// });
